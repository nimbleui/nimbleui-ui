{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "docs",
    "build",
    "scripts"
  ],
  "scripts": {
    "theme:prod": "lerna run --scope @yy/theme build",
    "theme:dev": "lerna run --scope @yy/theme dev",
    "docs:dev": "lerna run --scope @yy/docs docs:dev",
    "build": "lerna run --scope @yy/build build",
    "cli": "yy-cli"
  },
  "devDependencies": {
    "eslint": "^8.33.0",
    "lerna": "^6.1.0",
    "vue": "^3.2.45",
    "stylelint": "15.6.0"
  }
}
